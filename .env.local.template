# Copy this file: `cp .env.local.template .env.local` and fill in your project values.
# These vars are read at build time by Expo (anything prefixed with EXPO_PUBLIC_ is
# inlined into the JS bundle, so they MUST be safe to ship to clients).
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
